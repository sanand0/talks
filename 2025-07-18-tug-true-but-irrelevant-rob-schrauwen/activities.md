# Activities

1. **Use LLMs and RAG to answer Scopus queries from abstracts**
  - Who: speaker
  - When: before the talk
  - Model/tool: LLMs, retrieval augmented generation (RAG), Scopus.com
  - What happened: Rob described querying LLMs with abstract context and formatting answers from that context.
  - Source:
    > `we query these LLMs and then give the context from our own articles`
    > `we call that the retrieval augmented generation, or RAG.`

