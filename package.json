{
  "scripts": {
    "lint": "npx -y prettier --print-width 120 --write '**/*.md'",
    "build": "bash -lc 'shopt -s nullglob; for d in */; do [[ -f $d/README.md && -f $d/index.html && ( ! -f $d/index.html || $d/README.md -nt $d/index.html ) ]] && (cd \"$d\" && npx -y @marp-team/marp-cli@latest --html README.md -o index.html); done'"
  }
}
